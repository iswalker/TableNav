* See the example values when going to pivot -- no forgetting "which column was the important one"
* No more cluttering workbooks with 10 tabs of pivots; double click filters the source directly
* Filter faster than ever -- hover + spacebar to edit live, vs. many clicks
* Strip out all irrelevant fonts, borders, coloring that plagues Excels -- PURE DATA
* FAST -- quicker bootup than Excel
* No more fussing with column widths -- automatically get as much data in one view as possible
* Medians included as a standard calculation for a selected area, not just sum/count/average
* Tabs -- multiple CSV's in one window instance, easily switched with keyboard shortcuts
* Colors -- if hex codes are detected, show a preview!  Allow storing data cleanly, but still seeing what you're working on.

